<div class="container">
  <h3>Recently visited</h3>
  <div class="visited">
    {{#each visited}}
      <a href="{{url}}" class="trainer-card">
        {{trainer}}
        <img src="{{preview}}" alt="{{trainer}}">
      </a>
    {{/each}}
  </div>
</div>
